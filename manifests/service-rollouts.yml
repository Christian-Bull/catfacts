apiVersion: v1
kind: Service
metadata:
  name: catfacts-rollouts
  namespace: catfacts
  labels:
    app: catfacts
spec:
  ports:
  - port: 5001
    protocol: TCP
    targetPort: 5001
  selector:
    app: catfacts
  sessionAffinity: None
  type: ClusterIP